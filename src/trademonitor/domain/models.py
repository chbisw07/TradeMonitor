"""Domain model placeholders for future milestones."""

from dataclasses import dataclass


@dataclass(frozen=True)
class DomainModel:
    """TODO: replace with concrete domain models in TM1."""

    identifier: str
